package workman.model;

public class ParttimelistDAO {
	
	public static void addPTList(long textlist, String user_id, String company_name, int reviewnum, int reviewscore, String recruitstatus, String date, String empperiod) {
		EntityManager em = PublicCommon.getEntityManager();
		EntityTransaction tx = em.getTransaction();
		tx.begin();
		
		try {
			Parttimelist.
		}
	}
	
}

